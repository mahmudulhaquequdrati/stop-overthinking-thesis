# DECISIONS: every choice, with its reason

> **Rule: we only add rows.** When a decision changes, we **don't delete the old row**.
> We strike it through (~~like this~~) and add a new row below saying why it changed.
> So we always know *why* the project looks the way it does.
>
> Teacher questions about these choices: [qa/](qa/). Hard word? See [GLOSSARY.md](GLOSSARY.md).
>
> Note (2026-09-17): the numbers #28 and #29 were used twice by mistake.
> The two 2026-09-17 rows about GPU time are now **#30** and **#31**.

| # | Date | Decision | Why | Evidence |
|---|---|---|---|---|
| 1 | 2026-09-13 | **Start a new thesis, separate from CARR.** New folder `~/stop-overthinking/`. CARR (`~/thesis`) stays unchanged. | The user wants to *build an improvement*, not only measure. CARR's router added +0.0 points. The user wants to learn from zero in a clean project. | User request |
| 2 | 2026-09-13 | **$0 only.** Free Colab / Kaggle GPUs, free data and models from Hugging Face. | The user's requirement. | — |
| 3 | 2026-09-13 | **Topic: teach a small reasoning model to think shorter without losing accuracy** ("stop overthinking"). | We had 4 free ideas: stop overthinking, self-repair, early-failure alarm, prompt shrinker. This one has the clearest before/after number. It produces a real trained model. And many researchers work on it now. | Conversation, 2026-09-13 |
| 4 | 2026-09-13 | ~~**Code is the main topic; math is the transfer test.**~~ → replaced by #25 (code only; math transfer is future work) | Math is crowded (dozens of papers). Code has only ~3 training papers, all on older 7B models that always think. Code → math transfer: no paper found. Math → code is already done. | [research/gaps.md](research/gaps.md) |
| 5 | 2026-09-13 | ~~Math first, code as a transfer test~~ → replaced by #4. | The user asked "which is better?" The gap search showed the open gaps are in code. | [research/gaps.md](research/gaps.md) |
| 6 | 2026-09-13 | **Not Qwen3-1.7B.** | Too small and from 2025. We have a 15 GB GPU, so we can use a newer, bigger model. (The user noticed this.) | [research/models-and-gpu.md](research/models-and-gpu.md) |
| 7 | 2026-09-13 | **Not a 9B model** (e.g. Qwen3.5-9B). | Running it fits in 15 GB. But training it with 16-bit LoRA needs ~22 GB. Unsloth also advises against 4-bit training for Qwen3.5. | Unsloth Qwen3.5 fine-tune guide |
| 8 | 2026-09-13 | **Main model: `google/gemma-4-E4B-it`** (4-bit version, from Unsloth). | March 2026. Has a thinking switch. Apache-2.0 licence. **Published training cutoff: Jan 2025**, so test problems from 2025–26 are provably new to it. Unsloth has a free T4 notebook (10.7 GB peak memory, measured). | Hugging Face website + Unsloth docs |
| 9 | 2026-09-13 | **Backup model: `Qwen/Qwen3.5-4B`**, used if the memory check fails. | 9.3 GB file. Training with 16-bit LoRA measured 9.6 GB on a T4. Memory grows very little per token. Downside: no published cutoff date. | Hugging Face website + Unsloth docs |
| 10 | 2026-09-13 | **16 GB vs. 15 GB explained:** we load the squeezed 4-bit version (11.0 GB file), not the 16-bit one (16.0 GB). | Only 3.5B of the 8.0B numbers are squeezed to 4-bit. The word lookup tables and the image/audio parts stay 16-bit. Checked on the Hugging Face website. | Hugging Face file sizes |
| 11 | 2026-09-13 | **Train and test on easy + medium code problems only.** Hard problems are a written limitation. | Training fits ~3.5k tokens on a T4, but thinking on hard problems is 10–15k. A 4B model solves few hard problems, so there is nothing to learn from and nothing to measure. (The user proposed this.) | Conversation |
| 12 | 2026-09-13 | ~~**Hard-problem rule, set in advance:** a trial on 50 hard problems. ≥15% solved → add a small hard-problem check (H7). Otherwise not.~~ → replaced by #25 (hard problems dropped) | Let the data decide instead of guessing. | — |
| 13 | 2026-09-13 | **Training examples limited to ~3,500 tokens. At test time, every way of answering gets the same thinking limit (e.g. 8k).** | The 3,500 limit is a memory limit, for training only. The same test limit for everyone keeps the comparison fair. | Conversation |
| 14 | 2026-09-13 | **Selection rule: keep the shortest correct answer, but not shorter than half the median correct length.** | The S3-CoT paper (2602.01982) warns that training only on the very shortest answers hurts accuracy. The rule also stops the model learning "always stop early". | [research/data-size-and-test-size.md](research/data-size-and-test-size.md) |
| 15 | 2026-09-13 | ~~**Check H7′:** the trained model must still think longer on medium than on easy problems.~~ → replaced by #25 (H7′ dropped; the selection rule #14 still protects against stopping early) | Paper 2511.05874 found that cutting reasoning hurts harder tasks. We wanted to check the model doesn't cut corners where it shouldn't. | [research/gaps.md](research/gaps.md) |
| 16 | 2026-09-13 | **Two checks before the big runs:** memory check (≤14 GB) and room-to-shorten check (≥40% of problems solved at least once; shortest ÷ average length ≤0.75). | Nobody can promise an improvement. But we can check *before* spending GPU days whether there is something to learn. | [research/data-size-and-test-size.md](research/data-size-and-test-size.md) |
| 17 | 2026-09-13 | ~~**Learning curve at 100 / 250 / 500 / 1k / 2k examples** instead of guessing one data size.~~ → replaced by #25 (the learning curve is future work) | Papers used 400–8,000 examples, and none published this curve for shortest-correct training. The curve would have been our contribution (gap G-B). | [research/data-size-and-test-size.md](research/data-size-and-test-size.md) |
| 18 | 2026-09-13 | **Test set: ~1,000–2,000 code problems, compared on the same problems before and after (paired).** | A 2–3 point accuracy change needs ~1,250–1,900 problems to see with a paired comparison. A 30-problem contest set can't show it. | [research/data-size-and-test-size.md](research/data-size-and-test-size.md) |
| 19 | 2026-09-13 | **What we compare against (baselines): thinking OFF, thinking budget, "think briefly" prompt, thinking ON.** | If a free switch or prompt works as well, training adds nothing. That's the honest test (gap G-A). The NoThinking paper (2504.09858) showed OFF can beat a small thinking budget. | [research/gaps.md](research/gaps.md) |
| 20 | 2026-09-13 | **Don't claim:** first to shorten code reasoning, first on cheap GPUs, or math → code transfer. | SEER + ASAP, TokenSkip, and HAPO/LC-R1 already did these. Claiming them would fail the defence. | [research/gaps.md](research/gaps.md) |
| 21 | 2026-09-13 | **Never commit to git unless the user asks.** | The user's instruction. | CLAUDE.md §3 |
| 22 | 2026-09-13 | **Every session updates the docs** (ROADMAP, DECISIONS, PLAN, PAPERS, research/). | The user asked that everything said in chat is saved. | CLAUDE.md §2 |
| 23 | 2026-09-13 | **Teach first, then do.** One lesson at a time. The user explains each lesson back. We start at "What is an LLM?". | The user is a beginner and wants to know what, why and how at every step. | CLAUDE.md §1 |
| 24 | 2026-09-13 | ~~**Wrote the thesis proposal as a PDF** (`proposal/`), with the same section order as the old CARR proposal, easy English, a 12-week timeline, no names on the cover.~~ → replaced by #26 (shorter, black-and-white proposal) | The supervisor must accept the proposal. The old proposal's familiar shape (summary, gap table, questions, method, design, timeline, risks) helps. The "In simple words" boxes and word list made it readable for a beginner. Everything came from PLAN.md; nothing new was claimed. | [proposal/](proposal/) |
| 25 | 2026-09-13 | **One research question only: G-A, "is shortest-correct training better than the free options (thinking OFF, budget, 'think briefly')?"** One hypothesis in 3 parts: ≥25% fewer thinking tokens than thinking ON, ≤3 points accuracy loss, more accurate than the 3 free options. Moved to future work: learning curve (G-B), code → math transfer (G-C), LoRA size (G-D), fresh vs. seen problems (G-E), GRPO, hard-problem trial, H7′. | The user wants one study with the best chance of success, to turn into a conference paper. The method already cut tokens on math (Munkhbat et al.) and on 7B code (SEER), so savings are likely. G-A is the most clearly open gap. One training run fits the free GPU time. Success means a better balance, not higher accuracy than thinking ON. | User request; [research/gaps.md](research/gaps.md) |
| 26 | 2026-09-13 | **Rewrote the proposal: 4 A4 pages, black and white, easy English, no boxes or word list** (hard words explained in the text). 10 sections, 11 references. Same 12-week timeline. Weeks 1–2 now include learning the tools. | The user said the first proposal was too big and too colourful. | [proposal/](proposal/) |
| 27 | 2026-09-13 | **Fix set in advance, if the room-to-shorten check fails: ask each problem 8 times instead of 4**, then check again. | More tries give more chances to find a short correct answer. We fix it now, so the data decides, not our wishes. | — |
| 28 | 2026-09-13 | **New title: "Stop Overthinking, Keep Passing the Tests: Shortest-Correct LoRA Fine-Tuning versus the Thinking Switch in a Small Code Model (Gemma-4-E4B)".** Replaces ~~"Think Less, Code Just as Well: Teaching a Small Reasoning Model to Think Shorter on Code"~~. Changed in `proposal/proposal.html` + PDF (renamed `Stop_Overthinking_Keep_Passing_Proposal.pdf`), README, ROADMAP, PLAN, CLAUDE.md. | The user found the old title unattractive and too vague. It named neither the model, the training method, nor how correctness is judged. The new title says all four things the thesis does: cut unneeded thinking ("Stop Overthinking"), LoRA training on shortest correct answers, the model (Gemma-4-E4B), and grading by the benchmark's real tests ("Keep Passing the Tests"). The second half also names the contribution (gap G-A, the fair comparison with the thinking switch). The three dataset names were left out: they make the title clumsy, and they belong in the summary. The model is in brackets, so if the memory check (#16) fails and we switch to Qwen3.5-4B, only the bracket changes. | User request |
| 29 | 2026-09-13 | **Added `proposal/proposal.md`**, a Markdown copy of the proposal with the new title (#28). The PDF stays the version handed in. `proposal.html` stays the source the PDF is printed from. `old_proposal.docx` was **not** touched. | The user asked for the title in a Markdown version too, and there was none, only HTML and PDF. Markdown shows nicely on GitHub, so the proposal can be read without downloading anything. `old_proposal.docx` is the **CARR** proposal from the other project. Renaming it would change a historical document, and CLAUDE.md keeps CARR separate. Checked: the md has the same 10 sections, the same numbers and the same 11 references as the HTML. | User request |
| 30 | 2026-09-17 | **Plan ~4 GPU-hours (range 3–7 h) for the single LoRA training run: ~2,000 examples × ~1,800 tokens, 1 epoch (one pass through the data), one free T4.** Sort examples by length or pack them together. Save a checkpoint to Drive every ~30 min. Measure the real speed (tokens per second) during the week-3 memory check, then redo the number. *(Was numbered #28 by mistake.)* | The user asked how long training takes. The calculation: 3.6M tokens per pass ÷ ~250 training tokens per second. The speed comes from estimating the math per token (8 × 4e9 small math steps, "FLOPs") and how fast a T4 really works (6–12 trillion steps per second, "TFLOPS"). One pass follows Munkhbat et al. 2025. The run then fits one free session (Colab ≤12 h, Kaggle ~12 h). Written down so weeks 8–9 are planned on numbers, not hope. **The speed is not checked yet.** | [research/gpu-time-budget.md](research/gpu-time-budget.md) |
| 31 | 2026-09-17 | **Every GPU job can stop and continue later; no long jobs.** A "done" list + a results file on Google Drive that we only add to. Each result has a fixed name (`problem_id + sample_index + policy`). Each write is really saved to disk (flush/fsync). Each item has its own fixed seed. Training saves a checkpoint every ~30 min (the add-on, optimizer, scheduler, step, data order, random state). **Kaggle is the main GPU** (its "Save & Run All" runs with the browser closed, 2×T4). Colab is the second worker and the place to fix bugs. **Write many answers at once with vLLM before asking for more hours.** *(Was numbered #29 by mistake.)* | Free sessions are ~12 h and can end early, but the work needs tens of hours. Making data and testing are made of independent problems, so they split easily; only the bookkeeping was missing. Writing many answers at once is 2–5× faster and changes nothing in the science, so it comes before asking for more time. | [research/gpu-time-budget.md](research/gpu-time-budget.md) §8 |
| 32 | 2026-09-17 | **All docs rewritten in very simple English** (ROADMAP, PLAN, DECISIONS, README, PAPERS, lessons 00–01, all 5 research notes). Same facts and same numbers; only the words changed. New **[GLOSSARY.md](GLOSSARY.md)** explains every hard word. Hard project words were renamed: "policy" → "way of answering", "headroom" → "room to shorten", "coverage" → "share of problems solved at least once". | The user said the docs were too hard: they used words the user didn't understand. The user wants to understand every step. Numbers were checked before and after the rewrite, so nothing was lost. | User request |
| 33 | 2026-09-17 | **New folder [qa/](qa/): one Q&A file per step** ("what we did and why"), with the questions a teacher may ask, hard questions, and "checked vs. assumed". Files 01–10 cover every step so far. **Every future step ends with a new Q&A file.** | The user wants to be ready when the teacher asks "why did you do this?". Writing the answers down after each step also tests if we really understand the step. | User request |
| 34 | 2026-09-17 | **The proposal (`proposal/` md, html, pdf) was NOT rewritten.** | It is the version handed in. The three files must say exactly the same thing. It already uses easy English (#26). | — |
| 35 | 2026-09-17 | **Thinking length is measured in tokens, not seconds or words.** (Written down now; PLAN §5 already measured tokens.) | Seconds change with the computer (GPU, software, batching); the token count is the same on any computer, so the comparison is fair and repeatable. All our limits (3,500 training, 8,000 test, the thinking budget) and GPU memory are set in tokens. All 5 ways of answering use the same model, so the same tokenizer. Limitation: token counts can't be compared directly with papers that used other models. | [lessons/02-tokens.md](lessons/02-tokens.md), [qa/11](qa/11-measuring-thinking-in-tokens.md) |
| 36 | 2026-09-17 | **Gemma's cutoff is checked: January 2025, for the pre-training data.** We call it "strong evidence, not proof" in the thesis. Problems from January 2025 itself count as "maybe seen"; only February 2025 and later count as fresh. | Checked on the Hugging Face model card: "Our pre-training dataset … with a cutoff date of January 2025". The card gives no date for the later training stages (chat, reasoning), so claiming more would not be honest. Counting the cutoff month as "maybe seen" is the safe choice, and it matches the "Feb–Apr 2025" we already used. | Gemma model card; [lessons/06-cutoff-dates.md](lessons/06-cutoff-dates.md) |
| 37 | 2026-09-17 | **Gemma's thinking switch format is checked on the model card:** thinking ON = `<\|think\|>` at the start of the system prompt (in code: `enable_thinking=True`); the thinking appears between `<\|channel>thought` and `<channel\|>`. | Our thinking budget and our token counting both need to know where thinking starts and ends. Written down now, tested for real in lesson 11. | Gemma model card; [lessons/04-reasoning-models-and-thinking.md](lessons/04-reasoning-models-and-thinking.md) |
| 38 | 2026-09-17 | **First three notebooks written:** `08_python_basics.ipynb` (Python practice, no GPU), `10_look_at_the_data.ipynb` (check the test sets, no GPU), `11_first_model_call.ipynb` (load Gemma 4-bit on a free T4, thinking ON vs OFF, count thinking tokens, save raw answers). Notebooks 08 and 10 were **run and work**. Notebook 11 is **written but not run**. | The project rule is "learn first, then do": each notebook comes after the lesson that explains it. Notebook 11 needs a free GPU session, which only the user can start. It is built from two checked sources: Gemma's model card and Unsloth's official Gemma-4-E4B T4 notebook. | [notebooks/](notebooks/) · [research/2026-09-17-part2-tool-checks.md](research/2026-09-17-part2-tool-checks.md) |
| 39 | 2026-09-17 | **Generation settings for every way of answering: `temperature=1.0`, `top_p=0.95`, `top_k=64`, sampling on, fixed seeds, the same token limit.** Only the thinking switch (or the prompt, or the LoRA) may differ. Provisional: confirm in Part 4 before the real test runs. | These are Gemma's own recommended settings (model card "Best Practices" and `generation_config.json`), so nobody can say we made the base model look bad. Sampling is needed anyway, because we ask each problem 4 times and average. Keeping every other setting equal is what makes the 5 ways of answering comparable (PLAN §3). | Gemma model card; [notebooks/11_first_model_call.ipynb](notebooks/11_first_model_call.ipynb) |
| 40 | 2026-09-17 | **LiveCodeBench is loaded by downloading its `.jsonl` files directly** (`hf_hub_download`), not with `load_dataset`. | The dataset uses a Python loading script. Hugging Face's own viewer refuses to run it ("runs arbitrary Python code"), and newer `datasets` versions may refuse too. Downloading the file worked in notebook 10 on 2026-09-17. | [research/2026-09-17-part2-tool-checks.md](research/2026-09-17-part2-tool-checks.md) §3 |
| 41 | 2026-09-17 | **Counted: only 70 LiveCodeBench problems are both fresh and easy/medium** (31 easy + 39 medium; plus 61 hard), from February–April 2025. So the fresh set stays a **side note**, not the main test set; the main comparison keeps using the full paired test set. | We counted every row of `test5.jsonl` and `test6.jsonl` ourselves. A 2–3 point accuracy change needs about 1,250+ paired problems (DECISIONS #18), so 70 problems cannot carry the main result. This also confirms that "fresh vs. seen" stays future work (#25), and it is an honest limitation to write in the thesis. | [research/2026-09-17-part2-tool-checks.md](research/2026-09-17-part2-tool-checks.md) §3.1 |
| 42 | 2026-09-17 | **During training, the error score is counted only on the answer part, not on the question** (Unsloth's `train_on_responses_only`). This closes the open question in [qa/12](qa/12-how-our-training-works.md). | We don't want the model to learn to write questions; we want it to learn short correct answers. Unsloth's official Gemma-4 notebook does exactly this, so it is the tested path. To be re-checked when we build the training notebook (lesson 20). | Unsloth Gemma-4 E4B notebook |
| 43 | 2026-09-19 | **Notebook 11 loads Gemma with its per-layer word table (5.25 GiB) in CPU memory (`LOAD_MODE = "table_on_cpu"`), and runs on Kaggle, not Colab.** Backup: `"two_gpus"` (Kaggle 2×T4). If both fail, the memory check has failed → Qwen3.5-4B (PLAN §7). The "9.891 GB after loading" from Unsloth's older notebook no longer holds. | On a free Colab T4, loading crashed twice with "out of memory", the second time from a clean start (0 MiB used). Checked cause: the T4 has no bf16, so Unsloth 2026.9.7 converts every `embed_tokens…` module to float16 **on the GPU**. The per-layer table (262,144 × 42 × 256 × 2 bytes = 5.25 GiB, exactly the size in the error) then needs a second copy: 10.22 + 5.25 > 14.56 GiB. With the table on the CPU, the conversion happens there instead. It needs ~10.5+ GB of CPU memory: too tight on free Colab (~12.7 GB), fine on Kaggle (~29 GB, **not checked yet**). Kaggle is already our main GPU (#31). Side effect: ~5 GB more free on the GPU for the week-3 training check. **Not checked yet:** that this mode works end to end, and its speed. | [results/2026-09-19-notebook11-out-of-memory.md](results/2026-09-19-notebook11-out-of-memory.md) · Unsloth `patching_utils.py` · transformers 5.5.0 `quantizer_bnb_4bit.py` |
| 44 | 2026-09-19 | **On Colab, the model is downloaded from Hugging Face in every session. We don't upload it from a laptop, and we don't keep it on Google Drive.** On Kaggle, the model may later be attached once as an "Input" so new sessions don't download it. | Colab deletes everything when a session ends, including uploaded files. So uploading 11 GB from home would repeat every session, and a home upload is much slower than Google's own download from Hugging Face (e.g. 20 Mbit/s upload → ~75 min for 11 GB; Hugging Face → Colab usually a few minutes; **speeds not measured yet**). Keeping it on Drive would use ~73% of the free 15 GB, which we need for results and checkpoints. The real protection against a lost session is the resumable results file (#31), not a saved model. | User question, 2026-09-19 |
| 45 | 2026-09-20 | **For `table_on_cpu`, notebook 11 loads the model from a local folder of shortcuts (symlinks) to the downloaded files, with one changed `config.json`: `quantization_config.llm_int8_enable_fp32_cpu_offload = true`.** Adds to #43; #43 still holds. | The first Kaggle run stopped with "Some modules are dispatched on the CPU or the disk". We had passed the flag in code, but for an already-4-bit model transformers 5.5.0 keeps the model's own `quantization_config` and ignores the one passed in code (bitsandbytes settings are not merged; checked in `quantizers/auto.py`, `merge_quantization_configs`). Changing `config.json` in a shortcut folder is the smallest change: no second 11 GB download, no extra disk, the original files stay untouched. The folder keeps the model's name, because Unsloth reads "bnb-4bit" from it. **Not checked yet:** that the load now goes through. | Kaggle run 2026-09-20 · [results/2026-09-19-notebook11-out-of-memory.md](results/2026-09-19-notebook11-out-of-memory.md) |
| 46 | 2026-09-20 | **Notebook 11 ran on Kaggle with `table_on_cpu`: the loading fix (#43, #45) works, and the thinking switch works. But writing speed is ~4.4 tokens per second, one answer at a time. So before Part 4 we add a speed test: ask many questions at once (batching), and check whether vLLM runs Gemma-4 on a T4. The GPU-hour table (PLAN §10) is not valid until then.** Thinking ON is reported as an average that includes answers with 0 thinking tokens, and we also count how often that happens. | Measured in 4 answers: 307 thinking tokens (ON, try 0), **0** (ON, try 1: the model skipped thinking by itself, although the prompt had `<\|think\|>`), 0 and 0 (OFF). At 4.4 tokens per second, a 2,000-token answer takes ~8 min, and the ~40M tokens for training data would take ~2,500 GPU-hours instead of the planned 15–40. That is 60–170× too slow, so it must be fixed before any big run. The causes are **not checked yet**: one answer at a time, float32 on the T4, 4-bit unpacking, the table in CPU memory. Batching was already the plan (#31). Deciding about cuts or Qwen now would be guessing; the speed test gives the numbers. | [results/2026-09-19-notebook11-first-call.md](results/2026-09-19-notebook11-first-call.md) · raw: `results/2026-09-19-notebook11-raw.jsonl` |
| 47 | 2026-09-20 | **Keep Gemma-4-E4B. The speed fix to test is the engine that writes answers: llama.cpp with the GGUF Q4_K_M file (4.98 GB), many questions at once.** Training stays on Unsloth with the bnb-4bit file. All 5 ways of answering use the same engine and the same GGUF squeeze. **Provisional:** the speed test (notebook 12) decides. If llama.cpp can't run Gemma 4 with the thinking switch, or it is still far too slow, we look again, with E2B as the first fallback. | The user asked us to pick the best and fastest Unsloth Gemma model. Only Gemma 4 has the thinking switch, so older Gemma families are out. Of the 5 Gemma 4 sizes, 26B A4B and 31B don't fit a free T4 with room for thinking. 12B is much stronger (LiveCodeBench v6: 72% vs 52%), but it does ~3× more math per token and probably can't be trained on a T4 with 3,500-token examples. E2B is faster but weaker (44%), and its bnb file has the same 16-bit table problem. The model is not our speed problem: writing one answer at a time is (#46). A GGUF file also squeezes the big per-layer table, so no CPU trick is needed for answering. | [research/2026-09-20-which-gemma-model.md](research/2026-09-20-which-gemma-model.md) · Gemma 4 model card |
| 48 | 2026-09-20 | **The $0 rule is relaxed: a small paid Colab GPU package (Pro or Pay As You Go, about $10–20) may be used. Kaggle stays the free backup. No other paid services.** The thesis states which GPU each run used. | The user has **one week** to finish. On the free T4, Gemma 4 has no bf16, so it runs in float32 mode and needs the CPU-table trick, and it writes at 4.4 tokens per second (#46). A paid L4 or A100 has bf16 and more memory, and can use fast answer-writing tools. That removes the biggest risk to finishing on time. The user chose this knowing it breaks the old rule. Prices and GPU hours are **not checked yet**. | User decision, 2026-09-20 |
| 49 | 2026-09-20 | **The user's own Mac (Apple M2, 8 GB) becomes a second worker, running `unsloth/gemma-4-E2B-it-UD-MLX-4bit` with `mlx-lm`. Measured: 34 tokens/s alone, 59 tokens/s with 8 questions at once, under 4 GB of memory.** Which model the thesis finally uses (E2B on the Mac, or E4B on the paid GPU) is decided on Day 1, after a short accuracy pilot. | The user asked us to try this model, so we ran it: it is 8–13× faster than the free Kaggle T4 (4.4 tokens/s, #46), it has no session limit, it can run overnight, and it costs nothing. Three tool problems were found and fixed: load with `strict=False` (the last 20 of 35 layers share memory, so 140 saved numbers are unused), `batch_generate` needs token numbers not text, and mlx-lm 0.31.3 has a divide-by-zero bug in its speed counter (patched in memory). Also seen: E2B wrote 620–726 thinking tokens on the same question where E4B wrote 307, so there is a lot of room to shorten; and with thinking OFF it still wrote long chatty answers. E2B is weaker at code (44% vs 52% on LiveCodeBench v6, Google's numbers), so the model choice needs the pilot, not a guess. | [results/2026-09-20-e2b-mlx-on-mac.md](results/2026-09-20-e2b-mlx-on-mac.md) · raw + CSV in `results/` |
